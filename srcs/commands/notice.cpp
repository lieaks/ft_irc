#include "../../includes/commands.hpp"

bool	cmd_notice(Server &server, Client &client, std::vector<std::string> &input)
{
	(void)server;
	(void)client;
	(void)input;
	// should work for channel and client
	return true;
}
